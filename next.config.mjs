/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'images.unsplash.com',
            'lh3.googleusercontent.com',
            'i.pravatar.cc',
            'api.dicebear.com',
        ],
    },
};

export default nextConfig;
